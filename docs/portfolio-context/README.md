# Portfolio context (agent prompts)

Per-app markdown context for DevRoom agents. Formerly `projects/` at repo root — moved here in v3.1.0.

Agents like SCROLL reference these when updating Cap app documentation context.
