# Cap DevRoom — Sandboxes

**Copies only.** Agents read/write here — never `~/Desktop/Projects/` production folders.

## Sync

```bash
cd ~/Desktop/Cap-DevRoom
npm run sync:sandboxes
```

Copies: VaultCap, PulseCap, PrismCap, SteadyCap, LedgerCap, DeePonyCap, ScentCap, AuraCap.

Stale `*OS` sandbox folders are removed automatically.
