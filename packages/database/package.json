{
  "name": "@cap/devroom-database",
  "version": "3.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "prisma": "^5.22.0",
    "typescript": "^5.3.3"
  }
}
