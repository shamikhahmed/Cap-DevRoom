# Default SQLite database for local DevRoom.
# Path is relative to the Prisma schema dir (packages/database/prisma/),
# so ../../../data resolves to the repo-root data/ folder the app uses.
DATABASE_URL="file:../../../data/devroom.db"
